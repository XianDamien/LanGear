You are summarizing speaking feedback for one lesson.

Input JSON array:
{feedbacks_json}

Return JSON only with this schema:
{
  "overall": "overall performance summary",
  "patterns": ["recurring pattern 1", "recurring pattern 2"],
  "prioritized_actions": ["highest priority action", "second priority action"]
}

Rules:
- patterns max 3 items.
- prioritized_actions max 3 items.
- No numeric score.
